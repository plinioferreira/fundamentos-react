import React from "react"

export default function Primeiro(){
    const msg = 'Seja bem vindo(a)!'
    return (<div>
                Primeiro Componente
                <p>{msg}</p>
            </div>
            )
}